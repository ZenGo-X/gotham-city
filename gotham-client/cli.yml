name: aa-mpc demo
version: "1.0"
author: leontiad <leontiad@gmail.com>
about: Command Line Interface for a 2MPC sign protocol with L17
args:
  - verbose:
      short: v
      multiple: true
      help: Sets the level of verbosity
  - electrum-address:
      short: e
      multiple: false
      required: true
      value_name: ADDRESS
      takes_value: true
      help: Address of an electrum address to use. In the format of a sockaddress (url:port)
subcommands:
  - create-wallet:
      about: Create an MPC wallet
      version: "1.0"
      args:
      - verbose:
          short: v
          help: Sets the level of verbosity
  - wallet:
      about: Operation on wallet
      version: "1.0"
      args:
        - new-address:
              short: a
              help: Generate a new address
        - get-balance:
              short: b
              help: Total balance
        - list-unspent:
              short: u
              help: List unspent transactions (tx hash)
        - backup:
              short: s
              help: Private share backup
        - verify:
            short: c
            help: Backup verification
        - restore:
              short: r
              help: Private share recovery
        - rotate:
            short: o
            help: Private shares rotation
      subcommands:
        - send:
            about: Send a transaction
            version: "1.0"
            args:
              - to:
                  short: t
                  required: true
                  value_name: to
                  help: Receipient
              - amount:
                  short: a
                  required: true
                  value_name: amount
                  help: Amount in BTC